# Home Assistant Brands PR (ready)

Use these files to open a PR on `home-assistant/brands`:

- `custom_integrations/tunnel_proxy/icon.png`
- `custom_integrations/tunnel_proxy/logo.png`

Path in this repository:

- `brands_pr/custom_integrations/tunnel_proxy/icon.png`
- `brands_pr/custom_integrations/tunnel_proxy/logo.png`

## PR Title

Add brand images for custom integration `tunnel_proxy`

## PR Body

This PR adds brand images for the custom integration domain `tunnel_proxy`.

Files added:
- `custom_integrations/tunnel_proxy/icon.png`
- `custom_integrations/tunnel_proxy/logo.png`

Integration repository:
- https://github.com/edmondoalex/tunnel_proxy

Notes:
- Domain in `manifest.json` is `tunnel_proxy`.
- Images are PNG and intended for Home Assistant/HACS integration cards.

## Quick steps

1. Fork `home-assistant/brands`.
2. In your fork, create path `custom_integrations/tunnel_proxy/`.
3. Upload the two PNG files from `brands_pr/custom_integrations/tunnel_proxy/`.
4. Open PR to `home-assistant/brands` (`master` branch).
